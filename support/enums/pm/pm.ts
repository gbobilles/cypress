export enum Pm {
  PMS = 'PMs',
  GENERAL = 'General',
  PM_INTERVALS = 'PM Intervals',
  STANDARD_REPAIRS = 'Standard Repairs',
  DOCUMENTS = 'Documents',
  METRICS = 'Metrics',
  METRIC_DETAILS = 'Metric Details'
}
